# رقيم AI - Raqim AI

نظام مساعد ذكي شخصي لإدارة الأفكار والمشاريع وإنشاء المحتوى

## 🌟 المميزات

### 1️⃣ Raqim Brain (نظام التفكير الشخصي)
- إدارة المشاريع والجلسات
- تسجيل القرارات والملاحظات
- لوحة تحكم Dashboard بالعربية RTL
- إحصائيات شاملة

### 2️⃣ Workbench (ورشة العمل)
- نظام سير عمل عمودي للمحتوى
- 6 مراحل: الفكرة → البحث → المخطط → المسودة → التحسين → الجدولة
- واجهة Tabs لكل مرحلة
- حفظ التقدم لكل مرحلة

### 3️⃣ Control Layer (طبقة التحكم)
- تقييم جودة الردود تلقائياً
- كشف التكرار والتناقضات
- عرض نقاط الجودة (Quality Score)
- أزرار التحسين وإعادة التوليد

## 🚀 التشغيل

### المتطلبات
- Node.js 18+
- PostgreSQL (اختياري - يستخدم in-memory للتجربة)

### التثبيت

```bash
# تثبيت الحزم
npm install

# تشغيل خادم التطوير
npm run dev
```

### متغيرات البيئة

انسخ `.env.example` إلى `.env` وعدّل القيم:

```bash
cp .env.example .env
```

```env
DATABASE_URL=postgresql://localhost:5432/raqim
OPENAI_API_KEY=your_openai_api_key_here
PORT=3000
```

## 📁 هيكل المشروع

```
raqim-3CODE/
├── server/
│   ├── db/
│   │   ├── schema/
│   │   │   ├── brain.ts      # مخطط قاعدة بيانات Brain
│   │   │   └── workbench.ts  # مخطط قاعدة بيانات Workbench
│   │   └── index.ts
│   ├── routes/
│   │   ├── brain.ts          # API للمشاريع والجلسات والقرارات
│   │   ├── workbench.ts      # API لسير العمل
│   │   └── chat.ts           # API للمحادثة مع التقييم
│   ├── services/
│   │   └── evaluator.ts      # خدمة تقييم الجودة
│   └── index.ts
├── client/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── Brain.tsx     # صفحة لوحة تحكم Brain
│   │   │   ├── Workbench.tsx # صفحة ورشة العمل
│   │   │   └── Chat.tsx      # صفحة المحادثة
│   │   ├── components/
│   │   │   └── MessageInspector.tsx  # عرض تقييم الجودة
│   │   ├── App.tsx
│   │   └── main.tsx
│   └── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
└── drizzle.config.ts
```

## 🛠️ API Routes

### Brain API
- `GET /api/brain/projects` - قائمة المشاريع
- `POST /api/brain/projects` - إنشاء مشروع جديد
- `GET /api/brain/sessions` - قائمة الجلسات
- `POST /api/brain/sessions` - إنشاء جلسة جديدة
- `GET /api/brain/decisions` - قائمة القرارات
- `POST /api/brain/decisions` - إنشاء قرار جديد
- `GET /api/brain/stats` - إحصائيات Brain

### Workbench API
- `GET /api/workbench/workflows` - قائمة سير العمل
- `POST /api/workbench/workflows` - إنشاء سير عمل جديد
- `POST /api/workbench/state` - حفظ حالة المرحلة
- `GET /api/workbench/state/:workflowId/:stage` - استرجاع حالة المرحلة
- `GET /api/workbench/stages` - قائمة المراحل

### Chat API
- `POST /api/chat` - إرسال رسالة واستلام الرد مع التقييم
- `POST /api/chat/improve` - تحسين رسالة سابقة
- `GET /api/chat/history/:sessionId` - تاريخ المحادثة

## 🎨 التقنيات المستخدمة

### Backend
- Express.js
- Drizzle ORM
- PostgreSQL
- TypeScript

### Frontend
- React 18
- TypeScript
- Vite
- Tailwind CSS
- TanStack Query
- React Router
- Lucide Icons

## 📝 ملاحظات

- جميع النصوص بالعربية مع دعم RTL
- تقييم تلقائي لكل رسالة من المساعد
- يمكن استخدام النظام بدون قاعدة بيانات (in-memory)

## 📄 الرخصة

MIT License